#jdk:
#  -openjdk:11
#
#env:
#  JAVA_OPTS: "-Xmx2048m"
#  GRADLE_OPTS: "-Xmx2048m"
#
#
#before_install:
#  - sdk install java 11.0.12-open
#  - sdk use java 11.0.12-open
#  -

jdk:
  - openjdk11

env:
  JAVA_OPTS: "-Xmx2048m -Xms512m"
  GRADLE_OPTS: "-Xmx2048m -Xms512m"

before_install:
  - ./gradlew --version

install:
  - FILE ="-Dfile=netwrok_module-live-release.aar"
  - mvn install: install-file $File -DgroupId = com.github.mediqshc DartifactId = netwrok_module -Dversion =1.0.10
  - -DPackaging=aar -DgeneratePom=true
#  - ./gradlew clean -Pgroup=com.github.mediqshc -Pversion=1.0.6 -xtest -xlint assemble publishToMavenLocal
